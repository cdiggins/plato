using System.Collections.Generic;

namespace PlatoAbstractSyntax
{
    public class Evaluator
    {
        /*
        public dynamic Pop();
        public dynamic Push();
        public void Eval(string s)
        {
            switch (s)
            {
                case "+": return stack.Push(stack.Pop() + stack.Pop());
            }
        }
        */
    }
}