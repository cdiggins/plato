#version 300 es
// Autogenerated by Plato.GlslWriter: DO NOT EDIT
// Created on 2026-07-19 2:44:23 PM
precision highp float;
precision highp int;

// ---- Structs ----
struct Angle
{
    float Radians;
};

// ---- Function prototypes ----
float Pi();
float TwoPi();
float Add(float x, float y);
float Subtract(float x, float y);
float Multiply(float x, float y);
float Divide(float x, float y);
float Modulo(float x, float y);
float Negative(float x);
bool LessThan(float x, float y);
bool GreaterThan(float x, float y);
bool LessThanOrEquals(float x, float y);
bool GreaterThanOrEquals(float x, float y);
int Compare(float x, float y);
float Sqrt(float x);
float Abs(float x);
float Floor(float x);
float Pow(float x, float y);
Angle Acos(float x);
Angle Asin(float x);
Angle Atan(float x);
float Square(float x);
float Min(float a, float b);
float Max(float a, float b);
float Clamp(float x, float low, float high);
float Lerp(float a, float b, float t);
Angle Turns(float x);
Angle Degrees(float x);
int Compare(int x, int y);
bool And(bool x, bool y);
bool Or(bool x, bool y);
bool Not(bool x);
float Cos(Angle x);
float Sin(Angle x);
float Tan(Angle x);
vec2 UnitCircle(Angle theta);
vec2 Add(vec2 a, vec2 b);
vec2 Subtract(vec2 a, vec2 b);
vec2 Negative(vec2 v);
vec2 Scale(vec2 v, float s);
float Dot(vec2 a, vec2 b);
float Cross(vec2 a, vec2 b);
float LengthSquared(vec2 v);
float Length(vec2 v);
vec2 Normalize(vec2 v);
float Distance(vec2 a, vec2 b);
float DistanceSquared(vec2 a, vec2 b);
vec2 Lerp(vec2 a, vec2 b, float t);
vec2 MidPoint(vec2 a, vec2 b);
vec3 Add(vec3 a, vec3 b);
vec3 Subtract(vec3 a, vec3 b);
vec3 Negative(vec3 v);
vec3 Scale(vec3 v, float s);
float Dot(vec3 a, vec3 b);
vec3 Cross(vec3 a, vec3 b);
float LengthSquared(vec3 v);
float Length(vec3 v);
vec3 Normalize(vec3 v);
float Distance(vec3 a, vec3 b);
float DistanceSquared(vec3 a, vec3 b);
vec3 Lerp(vec3 a, vec3 b, float t);
vec3 MidPoint(vec3 a, vec3 b);
vec3 Min(vec3 a, vec3 b);
vec3 Max(vec3 a, vec3 b);
float MaxComponent(vec3 v);
vec3 Perpendicular(vec3 v);
vec3 Reflect(vec3 v, vec3 normal);

// ---- Function definitions ----
float Pi() { return 3.141592653589793; }

float TwoPi() { return 6.283185307179586; }

float Add(float x, float y) { return (x + y); }

float Subtract(float x, float y) { return (x - y); }

float Multiply(float x, float y) { return (x * y); }

float Divide(float x, float y) { return (x / y); }

float Modulo(float x, float y) { return mod(x, y); }

float Negative(float x) { return (-x); }

bool LessThan(float x, float y) { return (x < y); }

bool GreaterThan(float x, float y) { return (x > y); }

bool LessThanOrEquals(float x, float y) { return (x <= y); }

bool GreaterThanOrEquals(float x, float y) { return (x >= y); }

int Compare(float x, float y) { return (x < y ? -1 : (x > y ? 1 : 0)); }

float Sqrt(float x) { return sqrt(x); }

float Abs(float x) { return abs(x); }

float Floor(float x) { return floor(x); }

float Pow(float x, float y) { return pow(x, y); }

Angle Acos(float x) { return Angle(acos(x)); }

Angle Asin(float x) { return Angle(asin(x)); }

Angle Atan(float x) { return Angle(atan(x)); }

float Square(float x) { return (x * x); }

float Min(float a, float b) { return ((a < b) ? a : b); }

float Max(float a, float b) { return ((a < b) ? b : a); }

float Clamp(float x, float low, float high) { return ((x < low) ? low : ((x > high) ? high : x)); }

float Lerp(float a, float b, float t) { return ((a * (1.0 - t)) + (b * t)); }

Angle Turns(float x) { return Angle((x * 6.283185307179586)); }

Angle Degrees(float x) { return Angle((x * 0.017453292519943295)); }

int Compare(int x, int y) { return (x < y ? -1 : (x > y ? 1 : 0)); }

bool And(bool x, bool y) { return (x && y); }

bool Or(bool x, bool y) { return (x || y); }

bool Not(bool x) { return (!x); }

float Cos(Angle x) { return cos(x.Radians); }

float Sin(Angle x) { return sin(x.Radians); }

float Tan(Angle x) { return tan(x.Radians); }

vec2 UnitCircle(Angle theta) { return vec2(Cos(theta), Sin(theta)); }

vec2 Add(vec2 a, vec2 b) { return vec2((a.x + b.x), (a.y + b.y)); }

vec2 Subtract(vec2 a, vec2 b) { return vec2((a.x - b.x), (a.y - b.y)); }

vec2 Negative(vec2 v) { return vec2((-v.x), (-v.y)); }

vec2 Scale(vec2 v, float s) { return vec2((v.x * s), (v.y * s)); }

float Dot(vec2 a, vec2 b) { return ((a.x * b.x) + (a.y * b.y)); }

float Cross(vec2 a, vec2 b) { return ((a.x * b.y) - (a.y * b.x)); }

float LengthSquared(vec2 v) { return Dot(v, v); }

float Length(vec2 v) { return Sqrt(LengthSquared(v)); }

vec2 Normalize(vec2 v) { return Scale(v, (1.0 / Length(v))); }

float Distance(vec2 a, vec2 b) { return Length((b - a)); }

float DistanceSquared(vec2 a, vec2 b) { return LengthSquared((b - a)); }

vec2 Lerp(vec2 a, vec2 b, float t) { return (Scale(a, (1.0 - t)) + Scale(b, t)); }

vec2 MidPoint(vec2 a, vec2 b) { return Scale((a + b), 0.5); }

vec3 Add(vec3 a, vec3 b) { return vec3((a.x + b.x), (a.y + b.y), (a.z + b.z)); }

vec3 Subtract(vec3 a, vec3 b) { return vec3((a.x - b.x), (a.y - b.y), (a.z - b.z)); }

vec3 Negative(vec3 v) { return vec3((-v.x), (-v.y), (-v.z)); }

vec3 Scale(vec3 v, float s) { return vec3((v.x * s), (v.y * s), (v.z * s)); }

float Dot(vec3 a, vec3 b) { return (((a.x * b.x) + (a.y * b.y)) + (a.z * b.z)); }

vec3 Cross(vec3 a, vec3 b) { return vec3(((a.y * b.z) - (a.z * b.y)), ((a.z * b.x) - (a.x * b.z)), ((a.x * b.y) - (a.y * b.x))); }

float LengthSquared(vec3 v) { return Dot(v, v); }

float Length(vec3 v) { return Sqrt(LengthSquared(v)); }

vec3 Normalize(vec3 v) { return Scale(v, (1.0 / Length(v))); }

float Distance(vec3 a, vec3 b) { return Length((b - a)); }

float DistanceSquared(vec3 a, vec3 b) { return LengthSquared((b - a)); }

vec3 Lerp(vec3 a, vec3 b, float t) { return (Scale(a, (1.0 - t)) + Scale(b, t)); }

vec3 MidPoint(vec3 a, vec3 b) { return Scale((a + b), 0.5); }

vec3 Min(vec3 a, vec3 b) { return vec3(Min(a.x, b.x), Min(a.y, b.y), Min(a.z, b.z)); }

vec3 Max(vec3 a, vec3 b) { return vec3(Max(a.x, b.x), Max(a.y, b.y), Max(a.z, b.z)); }

float MaxComponent(vec3 v) { return Max(Max(v.x, v.y), v.z); }

vec3 Perpendicular(vec3 v) { return Normalize(((Abs(v.x) < 0.9) ? Cross(v, vec3(1.0, 0.0, 0.0)) : Cross(v, vec3(0.0, 1.0, 0.0)))); }

vec3 Reflect(vec3 v, vec3 normal) { return (v - Scale(normal, (2.0 * Dot(v, normal)))); }

