fix(csharp-writer): map concrete Array/Array2D/Array3D types to IReadOnlyList*

plato-src-v3 uses Array<T> directly in struct fields and signatures (plato-src
only ever goes through the IArray concepts, which the writer already renames).
ToCSharpTypeName now renders the concrete Array primitive types as the same
runtime interfaces (IReadOnlyList / IReadOnlyList2D / IReadOnlyList3D), removing
~1000 CS0308 errors from v3-generated C#.

Goldens byte-identical (regen-generated: 184/184 identical both variants),
frozen V1 untouched, PlatoTests 142/142, conformance 205/205.

v3 pipeline validation findings (not fixed here): generated v3 C# still does
not compile standalone or against Plato.Intrinsics.V2 — v3 needs its own
runtime/recipe (see report).

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
